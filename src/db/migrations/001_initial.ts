/**
 * Legacy Knex migration file.
 * Safely stubbed to prevent TypeScript compilation errors after removing the knex dependency.
 */
export async function up(): Promise<void> {
  // Migrations are now handled directly in Supabase SQL Editor.
}

export async function down(): Promise<void> {
  // Migrations are now handled directly in Supabase SQL Editor.
}
